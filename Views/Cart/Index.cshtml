@model List<RazorWebsite.Models.CartItem>

@{
    ViewData["Title"] = "Shopping Cart";  
} 
        <thead>
            <tr>
                <th>Product</th>
                <th>Quantity</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@(item.Product != null ? item.Product.Name : "Unknown Product")</td> <!-- Null check -->
                    <td>@item.Quantity</td>
                    <td>@item.TotalPrice.ToString("Ksh #,0")</td>
                    <td>
                        <form asp-controller="Store" asp-action="RemoveFromCart" method="post">
                            <input type="hidden" name="id" value="@item.ProductId" />
                            <button type="submit" class="btn btn-danger">Remove</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <a class="navbar-brand" asp-controller="Cart" asp-action="Index">
    <img src="path-to-cart-icon.png" alt="Cart" />
</a>
<a class="navbar-brand" asp-controller="Checkout" asp-action="Index">Checkout</a>


}
